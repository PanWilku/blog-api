{
  "name": "@blog-api/monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "version": "1.0.0",
"scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "dev": "concurrently \"npm run -w @blog/api dev\" \"npm run -w @blog/user dev\"",
  "build": "npm run -w @blog/api build && npm run -w @blog/user build && npm run -w @blog/admin build",
  "start": "npm run -w @blog/api start"
},
  "author": "panwilku",
  "license": "ISC",
  "description": "",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/icons-material": "^7.3.1",
    "@mui/material": "^7.3.1",
    "@toolpad/core": "^0.16.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "concurrently": "^9.2.1"
  }
}
